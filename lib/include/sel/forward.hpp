namespace sel {

  struct Val;
  struct Num;
  struct Str;
  struct Lst;
  struct Fun;
  struct Segment;

  struct NumLiteral;
  struct StrLiteral;
  struct LstLiteral;
  struct FunChain;

  struct NumDefine;
  struct StrDefine;
  struct LstDefine;
  struct FunDefine;

  struct Input;
  class App; // USL: remove if not needed in codegen

  struct NumResult;
  struct StrChunks;
  struct LstMapCoerse;

} // namespace sel
